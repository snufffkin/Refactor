# pages/overview.py
"""
Страница обзора программ
"""

import streamlit as st
import pandas as pd
import plotly.express as px
import plotly.graph_objects as go
import numpy as np

import core
from components.metrics import display_metrics_row, display_status_chart, display_risk_distribution
from components.charts import display_risk_bar_chart, display_metrics_comparison, display_success_complaints_chart
import navigation_utils
from components.utils import display_programs_by_class

# Словарь сокращённых названий программ (дополняйте по мере необходимости)
SHORT_PROGRAM_NAMES = {
    "Искусственный интеллект и генеративные нейросети": "ИИ и нейросети",
    "Курс Информатики для 5 класса 2023-2024 по ПРП. Программа «Один час в неделю»": "5 кл 23-24 ПРП 1ч",
    "Курс Информатики для 5 класса 2023-2024. Соответствует ФГОС. Программа «Один час в неделю»": "5 кл 23-24 ФГОС 1ч",
    "Курс Информатики для 7 класса 2021-2022": "7 кл 21-22",
    "Курс Информатики для 8 класса 2021-2022": "8 кл 21-22",
    "Курс Информатики для 8 класса 2021-2022. Программа «Один час в неделю»": "8 кл 21-22 1ч",
    "Курс Информатики для 8 класса 2023-2024. Программа «Два часа в неделю». Обновлённый вариант": "8 кл 23-24 2ч (обновл)",
    "Курс Информатики для 9 класса 2022-2023": "9 кл 22-23",
    "Материалы для подготовки к ОГЭ по информатике": "ОГЭ информатика",
    "Программа Информатики для 10 класса 2024-2025. Программа 1 час в неделю": "10 кл 24-25 1ч",
    "Программа Информатики для 11 класса 2024-2025. Программа 1 час в неделю": "11 кл 24-25 1ч",
    "Программа Информатики для 5 класса 2024-2025. Программа 1 час в неделю": "5 кл 24-25 1ч",
    "Программа Информатики для 6 класса 2024-2025. Программа 1 час в неделю": "6 кл 24-25 1ч",
    "Программа Информатики для 7 класса 2024-2025. Программа «Два часа в неделю»": "7 кл 24-25 2ч",
    "Программа Информатики для 7 класса 2024-2025. Программа «Два часа в неделю». ФГОС": "7 кл 24-25 2ч ФГОС",
    "Программа Информатики для 7 класса 2024-2025. Программа «Один час в неделю»": "7 кл 24-25 1ч",
    "Программа Информатики для 7 класса 2024-2025. Программа «Один час в неделю». ФГОС": "7 кл 24-25 1ч ФГОС",
    "Программа Информатики для 8 класса 2024-2025. Программа «Два часа в неделю»": "8 кл 24-25 2ч",
    "Программа Информатики для 8 класса 2024-2025. Программа «Два часа в неделю». ФГОС": "8 кл 24-25 2ч ФГОС",
    "Программа Информатики для 8 класса 2024-2025. Программа «Один час в неделю»": "8 кл 24-25 1ч",
    "Программа Информатики для 8 класса 2024-2025. Программа «Один час в неделю». ФГОС": "8 кл 24-25 1ч ФГОС",
    "Программа Информатики для 9 класса 2024-2025. Программа «Два часа в неделю»": "9 кл 24-25 2ч",
    "Программа Информатики для 9 класса 2024-2025. Программа «Два часа в неделю». ФГОС": "9 кл 24-25 2ч ФГОС",
    "Программа Информатики для 9 класса 2024-2025. Программа «Один час в неделю»": "9 кл 24-25 1ч",
    "Программа Информатики для 9 класса 2024-2025. Программа «Один час в неделю». ФГОС": "9 кл 24-25 1ч ФГОС",
    "Программа по информатике для СПО, 70 часов": "СПО 70ч",
}

def page_overview(df: pd.DataFrame):
    """Страница с обзором всех программ"""
    st.header("📊 Обзор программ")
    
    # Добавляем краткое описание
    with st.expander("ℹ️ О дашборде", expanded=False):
        st.markdown("""
        ### Дашборд качества курсов
        
        Этот дашборд помогает анализировать качество учебных материалов на всех уровнях:
        - **Программы**: Общий обзор всех программ
        - **Модули**: Детализация по модулям выбранной программы
        - **Уроки**: Анализ уроков в выбранном модуле
        - **Группы заданий (ГЗ)**: Детализация по группам заданий в уроке
        - **Карточки**: Подробный анализ отдельных заданий
        
        Используйте фильтры в боковой панели для навигации по уровням.
        
        **Ключевые метрики**:
        - **Успешность**: Процент успешных попыток решения заданий
        - **Жалобы**: Процент заданий, на которые поступили жалобы
        - **Дискриминативность**: Показатель способности задания различать знающих/незнающих студентов
        - **Риск**: Комплексный показатель проблемности задания (выше = хуже)
        - **Время**: Среднее время, затрачиваемое на выполнение задания
        """)
    
    # Добавляем метрику среднего суммарного времени на урок
    if 'time_median' in df.columns:
        programs_data = df.groupby("program").agg(
            total_time_median=("time_median", "sum"),
            lesson_count=("lesson", "nunique")
        ).reset_index()
        
        avg_time_per_lesson = programs_data["total_time_median"].sum() / programs_data["lesson_count"].sum() if programs_data["lesson_count"].sum() > 0 else 0
        avg_time_per_lesson = avg_time_per_lesson / 60
        
        # Отображаем метрику времени
        st.subheader("⏱️ Среднее время на урок")
        st.metric(
            label="Среднее суммарное время на урок (мин)",
            value=f"{avg_time_per_lesson:.1f}"
        )
    
    # 1. Отображаем общие метрики
    st.subheader("📈 Ключевые метрики")
    display_metrics_row(df)
    
    # 2. Отображаем распределение риска
    col1, col2 = st.columns(2)
    
    with col1:
        # Распределение риска по категориям
        display_risk_distribution(df, "program")
    
    with col2:
        # Статусы карточек
        display_status_chart(df, "card_id")
    
    # 3. Визуализируем программы в виде тримапа и рейтинга
    # col1, col2 = st.columns([3, 2])
    
    # with col1:
    #     # Топ программ с высоким риском
    #     high_risk_programs = display_risk_bar_chart(
    #         df, 
    #         "program", 
    #         title="Топ программ по уровню риска"
    #     )
    # Теперь график на всю ширину
    # Заменяем названия программ на сокращённые для графика, но сохраняем полное название для hover
    df_for_chart = df.copy()
    df_for_chart["program_full"] = df_for_chart["program"]
    df_for_chart["program"] = df_for_chart["program"].map(lambda x: SHORT_PROGRAM_NAMES.get(x, x))
    high_risk_programs = display_risk_bar_chart(
        df_for_chart, 
        "program", 
        title="Топ программ по уровню риска",
        height=800
    )
    
    # 4. Сравнение метрик для программ
    st.subheader("📊 Сравнение метрик по программам")
    
    tab1, tab2 = st.tabs(["Успешность и жалобы", "Метрики программ"])
    
    with tab1:
        # График сравнения успешности и жалоб
        display_success_complaints_chart(df, "program", limit=20)
    
    with tab2:
        # График сравнения нескольких метрик
        display_metrics_comparison(
            df,
            "program",
            ["success_rate", "complaint_rate", "discrimination_avg", "risk"],
            limit=10
        )
    
    # 5. Список программ с кликабельными ссылками
    st.subheader("📚 Список программ")
    
    # Группируем данные по программам
    agg = core.agg_by(df, "program")
    
    # Создаем таблицу с метриками
    st.dataframe(
        agg.style.format({
            "success": "{:.1%}", 
            "complaints": "{:.1%}", 
            "risk": "{:.2f}"
        }),
        use_container_width=True
    )
    
    # Отображаем программы, сгруппированные по классам
    display_programs_by_class(df, "program", metrics=["cards", "risk", "success"])
    
    # 6. Дополнительная аналитика
    if st.checkbox("Показать дополнительную аналитику", value=False):
        st.subheader("📋 Дополнительная аналитика")
        
        # Добавляем график общего соотношения статусов карточек
        status_counts = df["status"].value_counts().reset_index()
        status_counts.columns = ["Статус", "Количество"]
        
        fig = px.pie(
            status_counts, 
            values="Количество", 
            names="Статус",
            title="Общее распределение по статусам",
            color="Статус",
            color_discrete_map={
                "new": "#d3d3d3",
                "in_work": "#add8e6",
                "ready_for_qc": "#fffacd",
                "done": "#90ee90",
                "wont_fix": "#f08080"
            }
        )
        
        st.plotly_chart(fig, use_container_width=True)
        
        # Распределение типов карточек
        if "card_type" in df.columns:
            card_type_counts = df["card_type"].value_counts().reset_index()
            card_type_counts.columns = ["Тип карточки", "Количество"]
            
            fig = px.bar(
                card_type_counts,
                x="Тип карточки",
                y="Количество",
                title="Распределение типов карточек"
            )
            
            st.plotly_chart(fig, use_container_width=True)